# 贡献指南（CONTRIBUTING）

感谢你对本项目的关注。由于本项目属于**互操作性研究**，且包含第三方二进制，贡献时需要格外注意以下几点。

## 可以贡献什么

1. **对已有分析结论的修正**：如果你发现文档中的技术结论与字节证据不符，**非常欢迎**提交 Issue 或 PR。请附上：
   - 你使用的文件与其 SHA256；
   - 精确的**字节证据**：文件偏移 / RVA / 原始字节 / 解析命令与输出；
   - 你的复现步骤。
2. **新的分析工具**：通用的、只读的、可参数化的脚本（不要硬编码路径）。
3. **补充外部资料引用**：文档中标记为「需查外部资料」的问题，如果你能提供**规范/文档层面的明确依据**（含出处），欢迎补充。
4. **文档改进**：表述、结构、错别字。

## 贡献的硬性要求

### 证据要求（本项目最看重的一条）

- **每条结论必须可回溯**：给出「文件 + 偏移/行号 + 原始字节（或原文片段）」。
- **全称否定必须给三要素**：说「不存在 X」时，必须给出**搜索范围 + 匹配模式 + 命中数**（命中 0 也要写）。仅写「未找到」不被接受。
- **不得使用推测性措辞**。凡无法从材料中确定的，请明确标注为**未解**，并说明需要什么外部资料。
- **区分证据强度**：把「强结论」（如逐字节归属、计数封口）与「测量证据」（如统计特征）分别标注。

### 禁止事项

- **不得加入 NVIDIA 的任何受版权保护的二进制**（如 `nvngx_dlssnr.dll`）。
- **不得提交任何凭证**：API key、token、密码、私钥、证书。
- **不得提交含个人信息的绝对路径**（如带用户名的本地路径）。请使用仓库相对路径或命令行参数。
- **不得提交**：临时脚本、日志文件、编辑器/IDE 配置、`node_modules`、编译产物（`build/`、`*.pyc`、`*.pyo`）、`.git` 目录副本。
- **不得提交任何规避 DRM / 绕过许可校验的代码**。
- 新增第三方二进制**必须**同步更新 `EXTERNAL_BINARIES.md`（文件名、大小、SHA256、来源、原始许可证、用途），并在 `.gitattributes` 中确认已由 Git LFS 跟踪。

### 大文件

超过 GitHub 普通仓库限制的文件必须走 **Git LFS**：

```bash
git lfs install
git lfs track "binaries/*.dll"
git lfs track "binaries/*.bin"
git add .gitattributes
```

## 流程

1. Fork 本仓库并新建分支。
2. 提交改动，**在 PR 描述中说明**：改了什么、依据什么证据、如何复现。
3. 若你的改动推翻了既有结论，请在描述中明确写出「原结论 → 你的证据 → 更正后结论」。
4. 维护者会核对证据链。**结论正确但证据不足**的 PR 会被要求补充证据，而不是被直接拒绝。

## 报告问题

- **技术问题**：请附字节证据与复现步骤。
- **法律/版权问题**：见 [`LEGAL.md`](LEGAL.md) 第 6 条 —— 若版权方要求移除任何内容，我们将立即配合。请明确说明涉及哪个文件与依据。
